'use strict';

angular.module('cursAngularUpcApp', [
	'cursAngularUpcApp.recipes',
	'cursAngularUpcApp.home'
])

.config(function(/*Only providers*/) {
	
})

.run(function(/*Factories and services*/) {
	
})

;